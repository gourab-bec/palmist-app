{
  "name": "palmist-app",
  "version": "2.0.0",
  "description": "Palmly — AI palmistry readings with gated full reports, PDF export, and a daily horoscope subscription.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "build": "node build.mjs",
    "vercel-build": "node build.mjs",
    "test": "node --test tests/*.test.mjs",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "stripe": "^16.12.0"
  },
  "engines": {
    "node": "20.x"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
